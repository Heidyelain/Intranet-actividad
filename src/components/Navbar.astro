<nav class="fixed top-0 left-0 w-full bg-[#642D8E] text-white z-50">
  <div
    class="max-w-6xl mx-auto flex items-center justify-between px-4 py-3 md:px-8"
  >
    <!-- Logo -->
    <div class="flex items-center space-x-3">
      <img src="./imagenes/logo.png" alt="Logo" class="h-8 w-auto" />
    </div>

    <!-- Botón hamburguesa -->
    <div class="md:hidden">
      <button
        id="menu-toggle"
        class="focus:outline-none"
        aria-label="Abrir menú de navegación"
      >
        <svg
          class="w-6 h-6"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>

    <!-- Menú -->
    <ul
      id="menu"
      class="hidden md:flex md:space-x-6 absolute md:static top-full left-0 w-full md:w-auto bg-[#642D8E] md:bg-transparent flex-col md:flex-row items-start md:items-center space-y-1 md:space-y-0 text-sm font-medium px-4 md:px-0 pb-4 md:pb-0 z-50"
    >
      <li>
        <a href="#" class="px-4 py-2 hover:underline block w-full"
          >Nuestra Sociedad</a
        >
      </li>
      <!-- <li><a href="#" class="px-4 py-2 hover:underline block w-full">Nuestras Noticias</a></li> -->

      <!-- Nuestro bienestar -->
      <!-- <li class="relative group w-full md:w-auto">
        <div class="px-4 py-2 hover:underline block w-full cursor-pointer">Nuestro bienestar</div>
        <div
          class="hidden group-hover:flex flex-col absolute top-full left-0 bg-[#642D8E] min-w-max z-40">
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Política de estímulos</a>
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Actividades</a>
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Convenios</a>
        </div>
      </li> -->

      <!-- Así somos -->
      <!-- <li class="relative group w-full md:w-auto">
        <div class="px-4 py-2 hover:underline block w-full cursor-pointer">Así somos</div>
        <div
          class="hidden group-hover:flex flex-col absolute top-full left-0 bg-[#642D8E] min-w-max z-40">
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Homenajes</a>
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Equipo administrativo</a>
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Comité</a>
          <a href="#" class="block px-4 py-2 hover:underline whitespace-nowrap">Buzón de sugerencias</a>
        </div>
      </li> -->

      <!-- Botón -->
      <li class="w-full md:w-auto">
        <a
          href="https://actores.org.co/"
          class="bg-white text-[#642D8E] font-semibold px-5 py-2 rounded-full hover:bg-[#EB128A] hover:text-white transition-colors duration-200 block text-center mt-2 md:mt-0 md:inline-block"
        >
          Ir a Actores SCG
        </a>
      </li>
    </ul>
  </div>

  <!-- Script para menú hamburguesa -->
  <script>
    const toggle = document.getElementById("menu-toggle");
    const menu = document.getElementById("menu");

    if (toggle && menu) {
      toggle.addEventListener("click", () => {
        menu.classList.toggle("hidden");
      });
    }
  </script>
</nav>
